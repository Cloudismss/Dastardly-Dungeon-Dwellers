#include "Enemy.h"

// this is what the call will look like in DDD.cpp
/*
void inSomeFunction()
{
  Progression p;
  Enemy enemy;
  generateEnemy(p, enemy);
}

// enemyTier, rewardTier, enemyHealth, enemyName, enemyProgression, boss variables are placeholders until the rest of the code is refactored to account for Enemy and Progression classes
void generateEnemy(const Progression &p, Enemy &enemy)
{

}

void Enemy::setEnemyAttributes()
{

}

void Enemy::attackEnemy(float adjustment)
{
  health -= adjustment;
  if (health < 0)
  {
    health = 0;
  }
}
*/